import { Mulish, Leckerli_One } from 'next/font/google';

export const mulish = Mulish({ weight: ['800', '300', '500', '900', '700'], subsets: ['latin'] });
export const leckerli = Leckerli_One({ weight: ['400'], subsets: ['latin'] });